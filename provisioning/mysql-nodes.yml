---
- hosts: mysql_nodes
  pre_tasks:
    - name: Installing MySQL repository.
      yum: name=http://repo.mysql.com/mysql-community-release-el6-5.noarch.rpm state=present
  roles:
    - geerlingguy.mysql
  tasks:
    - include: tasks/mysql-ha-config.yml